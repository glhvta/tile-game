export const tilesSelector = state => state.tiles.allTiles;

export const activeTilesSelector = state => state.tiles.activeTiles;

export const tilesToRememberSelector = state => state.tiles.tilesToRemember;

export const levelSelector = state => state.level;

export const timeLineSelector = state => state.timeLine;

export const levelChangeModal = state => state.notifications.levelChange;

